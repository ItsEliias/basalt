# V3.1 — morning batch report

**Date:** 2026-09-01 · **Branch:** `v3-1-morning` (12 commits) · **Suite:** 800 → **853 green**.
Sequencing honored: deletion guard first · one collected rebuild · pure work during the
build · stop-point scripts handed at H1-bench + H2-first-route · H3/H4 proceeded regardless.

## The one rebuild

Native additions, collected as instructed: `react-native-vision-camera` 4.7.2 (+ config
plugin) and `react-native-worklets-core` (+ new babel.config.js). One version bump forced
by reality: worklets **1.6.2 → 1.6.3** — 1.6.2's CMake links the pre-RN-0.8x
`hermes-engine::libhermes` prefab and fails against RN 0.85 (caught because the first
"successful" install was actually the stale APK; the second build failed loudly and
1.6.3 fixed it). H2 needed **no** native addition (MapLibre's offline manager was already
in the app), H3 none (react-native-svg present), items 2/3/7 none. Everything after the
rebuild is JS-only over Metro, as promised.

## Small items

1. **Deletion guard** — did NOT already exist; built (5 tests): enumerates every
   `create table basalt_*` across migrations and fails the suite unless BOTH wipe paths
   cover it; two-sided tables pinned in both directions; `basalt_exercises` is the single
   allowlisted global table and the allowlist itself is pinned. **It caught its first
   real bug the same morning** — `basalt_ppg_calibration` landed without wipe coverage
   and the suite went red until both paths were extended. Working as designed.
2. **Breathing pacer** — already 80% shipped; the deltas: the circle became a scaling
   SQUARE with a 1 px stroke (the old "FOLLOW THE RING" was a forbidden-list violation
   in production copy — found and fixed), session lengths now 1–5 min per the brief.
   Haptics, silence, running state, pattern-named logging all stood.
3. **Tempo metronome** — published 3-1-1 (engine + tests, whole-second phases because the
   store ticks 1 Hz and sub-second would be precision theatre); beats during WORK only;
   catch-up ticks never burst haptics; off by default; per-exercise remembered.
4. **Nap credit** — was silently summing naps into nights (also polluting the need
   median). Now published: longest session = the night, extras ≤3 h = naps crediting the
   day, longer extras split-merge; the need MEDIAN reads nights only; math row shows
   "need 7:50 − nap 0:40 = 7:10 remaining" verbatim.
5. **Bedtime window** — target = need + min(1:00, debt÷3), anchored to the median of the
   user's own wake times. **Deviation:** the brief said "tomorrow's first planned item" —
   nothing in the ledger carries a clock time (programs/race plans are day-granular), so
   the wake habit is the honest anchor; stated in the srcnote. Hidden until 14 nights +
   7 wakes.
6. **Sleep consistency** — ±MAD of the last 14 bedtimes on an 18:00-origin clock axis
   (midnight can't split 11:50 pm/12:10 am into a 23-h spread — pinned). A spread, never
   a score.
7. **Weather line** — Open-Meteo confirmed keyless + attribution-only for non-commercial
   use; fetch fires only after the user's own Check GPS tap, coordinates rounded to
   ~1 km, attribution srcnote names exactly what left the device; no network → no line.
   Privacy page updated + pushed with a dated changelog entry.
8. **/formulas** — live at basalt.itseliias.com/formulas, generated by
   `scripts/generateFormulasPage.ts` from the same constants the app compiles against
   (readiness, ramp, mesocycle, TM percentages, sleep rules, gates, thresholds, Riegel…)
   — the page cannot drift from the code.

**Stretch item (micronutrient wall): not built** — the morning went to the heavy block;
it stays on the list.

## Heavy block

**H1 · Camera-PPG (flagship)** — pure analyzer (8 tests on synthetic waveforms with
construction-known RMSSD): detrend → smooth → peak detection with parabolic sub-sample
timing (the 30 fps grid alone injects ~13 ms of fake RMSSD; interpolation removes it,
pinned by a metronome fixture) → artifact-filtered RMSSD over clean successive pairs.
Published gates (SNR ≥2 · ≥30 clean beats · ≤20% artifacts · ≥30 s · ≥20 fps); any
failure → null + named reasons. Debug bench on Recover (dev builds): live waveform,
peaks, metrics, and `basalt_ppg_calibration` pairing each camera read with a typed-in
simultaneous watch RMSSD, mean |Δ| summarized.

> **Camera-vs-watch table: AWAITING YOUR PASTED RESULTS** (test script 1). My read will
> follow the data: mean |Δ| ≲ 12 ms over ≥3 clean pairs with honest discards on the bad
> reads → ship ON (feeding readiness with source `camera`, same 7-day baseline rules);
> worse or erratic → ship OFF behind the flag, keep the bench, gather more pairs.
> Discarded bad reads count FOR the feature — refusal is the product working.

**H2 · Offline tiles** — Stadia's ToS §8 (checked live 2026-09-01) **explicitly allows**
mobile offline caching ≤100 MB/device on any active plan; CARTO's keyless dev tiles do
not permit bulk download. So the corridor cache is provider-gated: Stadia key → cache
zooms 13–15 over the padded route bbox under a published 40 MB/route budget (blown
budget = pack deleted, refusal recorded), per-route MB shown, tap-to-remove; CARTO →
caching off with the terms named in the srcnote and route-line-only offline (which
worked before and still does). **Cache sizes: pending your Stadia key** (needs-you item
from V3) — the airplane-mode corridor test activates with it.

**H3 · Mobility v1** — nine stretches, three exact-total routines, transition floor
honored, haptic-only capable, assessment reorders emphasis and does nothing else (no
score exists in exports, copy, or schema — pinned three ways). **Deviation:** stretch
pose illustrations are the body-map figure highlighting target regions — the same
shipped visual language rather than nine new drawings this morning; proper thin-line
pose SVGs are a follow-up.

**H4 · %-of-training-max** — TM = 90% of trailing-8-week best e1RM, moves only by full
2.5 kg steps (both directions); phase percentages 70/72.5/75 · 82.5/85 · 60; the
suggestion line carries "72.5 kg = 85% of TM 85 kg" with the rule named inline.

## Deviations from the prompt, complete

1. Bedtime-window anchor: median wake, not "tomorrow's first planned item" (no clock
   times exist in the ledger).
2. Mobility pose art: body-map figure, not bespoke stretch SVGs.
3. H2's Stadia path is built but untestable without the key; CARTO fallback is the
   live behavior.
4. PPG bench gating: `__DEV__` for this morning; moves behind the Settings dev flag
   at tuning sign-off (as the brief specifies).
5. Worklets 1.6.3 bump (build-breaking incompatibility, not a choice).

## Still needs you (beyond the standing list)

- **Paste Script 1's calibration table** — decides H1 on/off.
- **Stadia key** in `app/.env` — activates H2's real path (also closes V3's tile-key item).
- Plug the phone in if the new build hasn't auto-installed yet (installer re-armed).
