# Secrets — never committed
.env
.env.local
.env.*.local

# Dependencies & build output
node_modules/
dist/
build/
.expo/
*.tsbuildinfo

# Expo continuous native generation — regenerated by `expo prebuild`;
# app/plugins/*.js is the durable source of truth for native changes.
app/android/
app/ios/

# Quarry porting reference (local only, never committed)
reference/

# Local tooling & runtime data (ruflo/claude session artifacts)
.claude/
.claude-flow/
.swarm/
.agents/
ruvector.db
basalt-handoff-pack/
basalt-handoff-pack.zip
agentdb.rvf
agentdb.rvf.lock

# OS
.DS_Store
supabase/.temp/
app/android/baseline-main-8323e32.apk

# Claude Code assistant/runbook config — internal, not for the public repo.
# Kept locally (still read by the tooling); removed from tracking. Note: it
# remains in prior git history — see below if a full scrub is wanted.
CLAUDE.md
